export class Resource {

  format: string;
  description: string;
  url: string;

  constructor(format: string, description: string, url: string) {
    this.format = format;
    this.description = description;
    this.url = url;
   }

}
